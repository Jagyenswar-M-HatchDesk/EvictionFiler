@* @implements CreateCase.IStepComponent

<div class="card p-4">
    <h4>Section 1: Case Type and Client Role</h4>

    <div class="form-check">
        <InputRadio name="role" @bind-Value="SelectedRole" Value="Landlord" class="form-check-input" />
        <label class="form-check-label">Landlord</label>
    </div>
    <div class="form-check">
        <InputRadio name="role" @bind-Value="SelectedRole" Value="Tenant" class="form-check-input" />
        <label class="form-check-label">Tenant</label>
    </div>

    <!-- More fields as needed -->

    <div class="d-flex justify-content-end mt-4">
        <button class="btn btn-secondary me-2" @onclick="OnSave">Save as Draft</button>
        <button class="btn btn-primary" @onclick="OnNext">Next</button>
    </div>
</div>

@code {
    [Parameter] public EventCallback OnSave { get; set; }
    [Parameter] public EventCallback OnNext { get; set; }

    public string Tenant { get; set; }

    private string SelectedRole = "";

    public Task SaveAsync()
    {
        // Save data here
        Console.WriteLine($"Saved role: {SelectedRole}");
        return Task.CompletedTask;
    }
}
 *@